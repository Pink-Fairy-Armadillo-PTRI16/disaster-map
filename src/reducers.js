import * as types from './actions';

const initialState = {

};

const mapReducer = (state = initialState, action) => {

  switch(action.type) {
    // case types.[insert type here]:
    // return {
    //   ...state,

    // }
  }

}

export default mapReducer;